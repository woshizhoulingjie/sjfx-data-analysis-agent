# 数据分析智能体 数据分析智能工作台：产品目标与实现方案

## 1. 项目定位

数据分析智能体 面向研究人员、分析师和业务团队，提供“文件理解 + 证据检索 + 交互式研究”的统一工作台。系统把本地文件、文件夹或数据包解析为可追溯的知识单元，用户可以用自然语言持续追问，快速定位原文证据、梳理人物与事件关系、发现邮件主题演化，并获得下一步可研究方向。

最终交付的不只是摘要，而是一套从原始材料到可验证结论的研究协作系统：每个判断都能回到来源文件、页码/段落或结构化字段；每次对话都能沉淀为报告、证据包和可复用的研究记忆。

## 2. 最终目标与用户价值

- **高质量文件处理**：支持 PDF、DOCX、XLSX/CSV、PPTX、TXT、Markdown、HTML、邮件及压缩包等常见格式；保留目录、页码、表格、附件和元数据。
- **交互式对话理解**：围绕单文件、文件夹或整个数据包对话，支持上下文追问、范围选择、对比、翻译、改写和报告生成。
- **深度挖掘邮件关系**：识别发件人/收件人、组织、项目、时间、主题和附件，构建通信网络、主题链和关键节点，支持按时间线与关系反查原文。
- **研究辅助**：从证据中提炼争议点、知识缺口、假设、研究问题、检索建议和后续行动，帮助用户从“读材料”进入“做研究”。
- **可审计输出**：答案带证据引用、置信度和局限说明，可导出 Markdown、DOCX、PDF、JSON 及证据包。

## 3. 核心能力

### 3.1 导入与解析

导入流水线负责路径校验、目录扫描、格式识别、解析隔离、编码处理、OCR/表格抽取和附件展开。对大目录和 GiB 级数据采用分批、断点续跑和后台队列，避免 Web 请求超时。

### 3.2 统一知识模型

将文档切分为文档、章节、页、段落、表格、邮件、附件和逻辑单元；为每个单元生成稳定 ID、来源定位、时间、作者、实体、标签和哈希。结构化结果与原文证据并存，便于精确问答和重新索引。

### 3.3 检索与问答

采用关键词、语义向量、元数据过滤和层级摘要的混合检索。分析规划器先判断问题类型和所需范围，再调用检索、统计、关系分析、时间线、翻译等工具；回答阶段执行证据拼接、主张验证和引用生成，降低幻觉。

### 3.4 邮件关系与主题分析

抽取邮件头、线程、参与者、组织、主题、时间和附件关系，生成：通信网络图、主题聚类、线程时间线、关键人物、异常往来和跨文件关联。用户可从图谱节点跳转到原始邮件和上下文证据。

### 3.5 研究方向生成

基于已确认事实、冲突证据和空白字段，输出候选研究问题、可检验假设、需要补充的数据、建议方法、优先级和风险；每个方向标注依据，避免把推测表述为事实。

### 3.6 导出与协作

支持生成研究摘要、深度分析报告、证据清单、关系数据和机器可读 JSON；导出内容包含来源定位、处理时间、模型信息和质量提示，便于复核、归档与二次使用。

## 4. 技术实现

系统采用 Web API + 后台 Worker + 持久化存储的架构。`app.py` 提供会话、导入、分析和导出接口；Worker 负责大任务和队列；`services/` 中的 scanner、import_pipeline、unified_parser、folder_analysis、package_analysis、retrieval、structured_qa、evidence_ledger、claim_verifier、conversation、reporting 等模块分别承担扫描解析、分析规划、检索问答、证据校验、对话和报告生成。

推荐处理链路如下：

1. **创建数据包**：校验路径是否在允许范围，记录任务和权限。
2. **扫描与分流**：识别文件类型、大小、编码和压缩层级，生成可重试任务。
3. **解析与标准化**：按格式提取文本、表格、图片/OCR、邮件头和附件，统一为逻辑单元。
4. **质量检查**：检测空文本、乱码、重复、解析失败和敏感信息，保留失败原因。
5. **索引与画像**：建立倒排/向量索引、实体索引、时间线、主题和关系画像。
6. **规划分析**：将用户问题拆成检索、统计、关系或验证步骤，限定证据范围。
7. **证据回答**：召回候选片段，重排并生成带引用的答案；对关键主张执行一致性校验。
8. **交互追问**：保留会话上下文和用户当前选择，支持缩小/扩大范围、比较和反查。
9. **沉淀输出**：保存研究记忆、报告、证据包和审计日志，支持再次分析。

### 4.1 模型与运行策略

模型通过 Ollama/vLLM 等适配层调用，可按任务选择轻量模型或高质量模型；离线翻译、嵌入、摘要和生成彼此解耦。默认先用确定性规则和结构化抽取，再让模型解释和归纳，以控制成本、延迟和可复现性。

### 4.2 性能与可靠性

大文件采用流式读取、分块解析、缓存和渐进式分页；任务具备状态、进度、重试、取消和锁。Web 层只负责提交与查询，Worker 负责长任务。关键结果写入证据账本，确保服务重启后可以恢复。

## 5. 典型工作流程

用户选择文件或目录 → 系统扫描并展示数据包概览 → 用户确认分析范围 → 系统生成结构化画像和可搜索索引 → 用户提出问题 → Agent 规划并检索证据 → 返回答案、引用、关系图/时间线和不确定性 → 用户继续追问或调整范围 → 一键生成研究报告和证据包。

## 6. 能力边界

- 结论以导入材料为依据；没有来源的内容只能作为假设或检索建议。
- OCR、扫描件、手写体、复杂版式和损坏文件可能降低准确率，系统必须标记解析质量。
- 实体消歧、邮件线程合并和关系推断存在误差，应提供原文回链和人工修正入口。
- 研究方向是辅助建议，不替代法律、财务、医学或安全等领域的专业判断。
- 默认不跨数据包混用证据，不自动修改原始文件，不向外部服务发送材料，除非用户明确配置并授权。
- 超大数据包受限于存储、索引和模型吞吐；通过分层分析、抽样和后台任务控制资源。

## 7. 特色优势

1. **证据优先**：答案、摘要和研究方向均可回链到精确来源，适合审计与学术复核。
2. **从文件到关系网络**：同时理解正文、表格、邮件头、附件和目录结构，支持跨文件关联。
3. **渐进式深度**：先快速概览，再按需深入单文件、主题、人物或时间段，兼顾速度与深度。
4. **Agent 工具编排**：根据问题动态组合检索、统计、时间线、关系分析和验证工具，而非单一聊天模型。
5. **大数据包工程能力**：后台队列、断点续跑、解析隔离、缓存和渐进分页保障长任务稳定性。
6. **研究闭环**：从事实提取、冲突发现到研究问题和报告导出，形成可重复的工作流。
7. **可控与可部署**：支持本地/内网模型和存储，权限、路径、日志和数据生命周期可配置。

## 8. 与同类产品对比

| 对比维度 | 数据分析智能体 | 传统 BI/可视化平台 | 数据分析 SaaS/低代码分析平台 | 企业知识库问答系统 | 专业邮件分析工具 |
|---|---|---|---|---|---|
| 主要处理对象 | 非结构化文件、邮件、表格、附件及混合数据包 | 结构化数据库和指标表 | 结构化数据、业务报表 | 已切分的文档知识库 | 邮箱及邮件元数据 |
| 文件解析深度 | 还原目录、页码、表格、附件和邮件线程 | 依赖外部 ETL，原文还原弱 | 以表格导入为主 | 文本切片为主 | 文件类型和附件分析有限 |
| 分析交互方式 | 自然语言连续对话，自动规划检索、统计、关系和验证步骤 | 拖拽看板、固定指标和筛选器 | 自助建模、问数和看板 | 基于文档的问答 | 搜索、筛选和邮件统计 |
| 证据可追溯 | 精确到文件/页/段/字段，答案附证据并校验主张 | 指标可追溯，原文证据较少 | 依赖数据血缘，难回到原始语境 | 可引用文档片段，但缺少推理链 | 通常只能定位邮件 |
| 关系与主题挖掘 | 人物、组织、项目、主题、时间线、附件跨文件关联 | 需预先建模，关系分析成本高 | 关系能力依赖数据模型 | 通常没有邮件网络 | 聚焦邮箱内关系，跨文件弱 |
| 研究辅助 | 从事实、冲突和空白生成研究问题、假设和后续方向 | 以监控和报表为主 | 以业务决策指标为主 | 以知识查询为主 | 以沟通管理为主 |
| 大数据包工程能力 | 后台队列、分块解析、断点续跑、渐进分页 | 依赖数仓和 ETL 团队 | 受平台配额和导入限制 | 通常面向中小规模文档 | 受邮箱平台规模限制 |
| 部署与隐私 | 支持本地/内网模型与存储，数据边界可控 | 企业部署成熟，建设成本较高 | 多为云服务，受供应商约束 | 取决于云端或私有化方案 | 依赖邮箱平台权限 |
| 输出形式 | 研究报告、证据包、关系数据、JSON | 仪表板、图表和报表 | 看板、报表和数据集 | 问答记录和文档摘要 | 邮件列表和统计 |

## 9. 评估指标与演进路线

建议持续跟踪解析成功率、引用命中率、主张验证通过率、问答满意度、任务 P95 延迟、单位 GiB 成本和人工修正率。演进优先级：完善邮件与附件解析 → 提升实体消歧和关系图交互 → 增加主动学习与人工反馈 → 引入多租户权限和审计 → 建设领域模板与评测集。

## 10. 一句话说明

数据分析智能体 是一个以证据为核心、能处理复杂文件和邮件关系、通过自然语言持续对话并把材料转化为研究洞见的智能分析工作台。
